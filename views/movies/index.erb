<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Movies</title>
  </head>
  <body>
    <h1>Movies</h1>
    <p>
    <% @movie_list.each do |movie| %>
    <li><a href="/movies/<%= movie["id"] %>"><%= movie["title"] %></a> | Year: <%= movie["year"] %> | Genre: <%= movie["genre"] %> | Rating: <%= movie["rating"] %> | Studio: <%= movie["studio"] %></li>
    <% end %>
    </p>
  </body>
</html>
